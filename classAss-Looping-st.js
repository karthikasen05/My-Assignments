function printodd() {
    

for (let i = 0; i <= 25 ; i++) {

    if (i%2!=0) {
        console.log (i);
        
    }

        
    }
}
    printodd();

    // ternary operator //
    
    let x = -25;
    function num() {
        let message = x >= 0 ? "number is postive" : "number is negative";
        console.log (message);

        
    }
    num();